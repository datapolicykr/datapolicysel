# Frozen release — 2026-09-09

This snapshot is the approved VAN survey design baseline.

Frozen scope:
- Tab 2: responsive comparison table, centered cell text, BTS reference section.
- Tab 3: reference dashboard layout and Korea map traced from the user-supplied image `1000043591.jpg`.
- Existing Admin login and DB management behavior must remain unchanged.
- Supabase schema/data and admin APIs are outside the visual-design freeze and must not be reset by design edits.

Operational site target: `van.datapol.chatgpt.site`
Source repository: `datapolicykr/datapolicysel`
Branch: `main`

Do not replace the frozen Tab 2/Tab 3 design components unless the user explicitly requests a new revision.
